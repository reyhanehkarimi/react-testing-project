import { render, screen } from "@testing-library/react";
import fetchMock from "jest-fetch-mock";
import data from "./data.json";
fetchMock.enableMocks();

describe("App component", () => {

  it("it displays a list of users", async () => {
    
  });
});